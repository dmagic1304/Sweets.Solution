@{
  Layout = "_Layout";
}

@using Sweets.Models

<h1>Welcome to Pierre's Sweet and Savory Treats!</h1>

  @* @if(Model.Count == 0)
  {
    <p>No recipes added yet.</p>
  }
  else
  {
  @foreach (Flavor recipe in Model)
    {
      <li>@Html.ActionLink(recipe.Name, "Details", new { id = recipe.RecipeId })</li>
    }
  } *@

